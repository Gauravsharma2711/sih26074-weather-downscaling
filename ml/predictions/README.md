# ML Predictions & Inference Layer

## Purpose
The `ml/predictions/` module exposes `DownscalingPipeline`, which coordinates cleaning, feature extraction, and inference into a single callable object used by the backend and advisory services.
